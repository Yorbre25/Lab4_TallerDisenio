module contadorXY (input logic clk_25,
						output [9:0]cuentaX,
						output [9:0]cuentaY);
				
				always @(posedge clk_25)
				
				if (cuentaX == 800) begin //cuando el contador X llega al maximo entonces cuentaX se vuelve 0 y le suma uno al contador y.
					cuentaX <= 0;
					y++;
					
					if (cuentaY == 525) // cuando el contador y llega a su maximo entonces cuentaY se hace 0.
				else
					cuentaX++;

endmodule
